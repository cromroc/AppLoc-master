<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.dan.myapplication.ui.SettingsActivity">

    <LinearLayout
        android:id="@+id/serverSettings"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_2lines"
        android:orientation="vertical"
        android:gravity="center_vertical">
        <TextView
            android:textColor="@color/primaryText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/list_item_title_text"
            android:layout_marginLeft="@dimen/spacing_large"
            android:layout_marginStart="@dimen/spacing_large"
            android:text="@string/settings_server_config"/>

        <TextView
            android:id="@+id/serverSettingsLabel"
            android:textColor="@color/secondaryText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/spacing_large"
            android:layout_marginStart="@dimen/spacing_large"
            android:layout_marginTop="@dimen/spacing_micro"
            android:textSize="@dimen/list_item_desc_text"
            tools:text="127.0.0.1:80"/>
    </LinearLayout>

</LinearLayout>
